import Layout from '@components/Layout';
import Archive from '@domain/저장하는/components/Archive';

const Page = () => {
  return (
    <Layout>
      <Archive />
    </Layout>
  );
};

export default Page;
