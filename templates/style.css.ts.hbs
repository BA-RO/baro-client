import { style } from '@vanilla-extract/css';
import { recipe } from '@vanilla-extract/recipes';

import * as utils from '@styles/utils.css';

export const {{name}}Style = style({});

export const {{name}}Recipe = recipe({
  base: {},
  variants: {},
});
